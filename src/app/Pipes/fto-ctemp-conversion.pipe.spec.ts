import { FtoCtempConversionPipe } from './fto-ctemp-conversion.pipe';

describe('FtoCtempConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new FtoCtempConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
